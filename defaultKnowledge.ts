
import { KnowledgeItem } from './types';

/**
 * INSTRUCTIONS FOR DR. MASUM:
 * 1. Open the app in Admin Mode (?admin=true).
 * 2. Upload your documents.
 * 3. Click "Copy Data for Deployment" in the Knowledge Base section.
 * 4. Paste the result below, replacing the empty array [].
 */
export const defaultKnowledge: KnowledgeItem[] = [];
